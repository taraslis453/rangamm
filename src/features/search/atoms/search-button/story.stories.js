import React from 'react';
import { SearchButton } from './index'
export default {
    title: 'features/search/atoms/SearchButton',
    component: SearchButton,
};

const Template = () => <SearchButton />;

export const Default = Template.bind({});