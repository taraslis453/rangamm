import React from 'react'
import {Title} from './index'

const title = {
  title: 'features/search/features/filter/atoms/Title',
  component: Title,
}

export default title

const Template = args => <Title>Title</Title>
export const Default = Template.bind({})
