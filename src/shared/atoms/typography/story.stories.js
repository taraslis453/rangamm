import React from 'react';
import { Typography } from './index';

export default {
    title: 'shared/atoms/Typography',
    component: Typography,
}

const Template = () => <Typography>Typography</Typography>
export const Default = Template.bind({});