import React from 'react';
import { Fixed } from './index';

export default {
    title: 'shared/atoms/Fixed',
    component: Fixed,
}

const Template = () => <Fixed>Fixed</Fixed>
export const Default = Template.bind({});