import React from 'react';
import { Flex } from './index';

export default {
    title: 'shared/atoms/Flex',
    component: Flex,
}

const Template = () => <Flex>Flex</Flex>
export const Default = Template.bind({});