import React from 'react';
import { Box } from './index';

export default {
    title: 'shared/atoms/Box',
    component: Box,
}

const Template = () => <Box>Box</Box>
export const Default = Template.bind({});